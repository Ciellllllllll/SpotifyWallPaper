import { defaultWallpaperPreferences } from '@spotify-wallpaper/shared-types';

/** Runtime settings authority. Legacy input is migrated before reaching this object. */
export const defaultSettings = defaultWallpaperPreferences();
