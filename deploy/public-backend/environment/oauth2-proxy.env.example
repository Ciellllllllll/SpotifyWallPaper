# Install as /etc/spotify-wallpaper/oauth2-proxy.env with root:root 0600.
# Replace every placeholder locally. Never add the installed file to an artifact.
OAUTH2_PROXY_CLIENT_ID=REPLACE_AT_INSTALL
OAUTH2_PROXY_CLIENT_SECRET=REPLACE_AT_INSTALL
OAUTH2_PROXY_COOKIE_SECRET=REPLACE_AT_INSTALL
